import './Header.css';

function Header() {
  return (
    <header className="App-header">
      <h2>Classificados</h2>
      <button class="card" id="btn-add">
        + Novo classificado
      </button>
    </header>
  );
}

export default Header;
